apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventflow-mcp.fullname" . }}
  labels:
    {{- include "eventflow-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "eventflow-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
